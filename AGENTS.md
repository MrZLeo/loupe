# Agent Instructions

- Use Conventional Commits for commit messages: `type: lowercase summary`.
- Prefer existing project types such as `feat`, `fix`, `refactor`, `build`,
  `test`, and `docs`.
- Do not use title-case commit subjects without a type prefix.

